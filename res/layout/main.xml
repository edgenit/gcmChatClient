<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
<!--
    <android.support.v7.widget.Toolbar
            android:id="@+id/my_toolbar"
            android:title="@string/app_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            />
            -->
    <include layout="@layout/toolbar"/>
    <FrameLayout
            android:id="@+id/fragmentParentViewGroup"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent" android:layout_below="@+id/my_toolbar"
            android:layout_alignParentStart="true">
    </FrameLayout>
    <!--
            <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textAppearance="?android:attr/textAppearanceLarge"
                    android:text="@string/AppName"
                    android:id="@+id/textView3" android:layout_gravity="left|top" android:layout_alignBottom="@+id/my_toolbar"
                    android:layout_alignParentStart="true" android:layout_below="@+id/my_toolbar"
                   />
    -->
</RelativeLayout>